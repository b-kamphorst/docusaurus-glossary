# Examples

We provide some example docusaurus projects ter reference of how to use the plugin.

- [Simple](./simple/) shows default usage.
- [Custom paths](./custom-paths) shows how to configure the `docusaurus-glossary` plugin if a custom `baseUrl`, `docs.path`, `docs.routeBasePath` and/or `glossaryPath` are used in the project.
  i
